export interface SelectApplyState {
  title: string;
  setTitle: (title: string) => void;
  resolution: string;
  setResolution: (resolution: string) => void;
  role: string;
  setRole: (wantedRole: string) => void;
}
